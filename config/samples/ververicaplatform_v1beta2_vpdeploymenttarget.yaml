apiVersion: ververicaplatform.fintechstudios.com/v1beta2
kind: VpDeploymentTarget
metadata:
  name: top-speed-v2
  namespace: top-speed
spec:
  metadata:
    namespace: default
  spec:
    kubernetes:
      namespace: top-speed
